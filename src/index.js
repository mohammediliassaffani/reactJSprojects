import React from "react";
import ReactDOM from "react-dom";

const customStyle = {
  color: "red"
};

ReactDOM.render(
  <div>
    <h1 style={customStyle}>hi from tanger</h1>{" "}
  </div>,
  document.getElementById("root")
);
